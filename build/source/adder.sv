/*
    This file was generated automatically by Alchitry Labs 2.0.52-BETA.
    Do not edit this file directly. Instead edit the original Lucid source.
    This is a temporary file and any changes made to it will be destroyed.
*/

module adder #(
        parameter SIZE = 6'h20
    ) (
        input wire [(SIZE)-1:0] a,
        input wire [(SIZE)-1:0] b,
        input wire alufn0,
        output reg [(SIZE)-1:0] s,
        output reg z,
        output reg v,
        output reg n
    );
    logic [31:0] R_169eeb30_i;
    logic [31:0] RR_169eeb30_i;
    localparam _MP_SIZE_1070529413 = SIZE;
    logic [(_MP_SIZE_1070529413)-1:0] M_rca_unit_a;
    logic [(_MP_SIZE_1070529413)-1:0] M_rca_unit_b;
    logic M_rca_unit_cin;
    logic [(_MP_SIZE_1070529413)-1:0] M_rca_unit_s;
    
    rca #(
        .SIZE(_MP_SIZE_1070529413)
    ) rca_unit (
        .a(M_rca_unit_a),
        .b(M_rca_unit_b),
        .cin(M_rca_unit_cin),
        .s(M_rca_unit_s)
    );
    
    
    logic [(SIZE)-1:0] xb;
    logic [(SIZE)-1:0] sum;
    always @* begin
        for (RR_169eeb30_i = 0; RR_169eeb30_i < SIZE; RR_169eeb30_i = RR_169eeb30_i + 1) begin
      R_169eeb30_i = (0) + RR_169eeb30_i * (1);
            xb[R_169eeb30_i] = b[R_169eeb30_i] ^ alufn0;
        end
        M_rca_unit_a = a;
        M_rca_unit_b = xb;
        M_rca_unit_cin = alufn0;
        sum = M_rca_unit_s;
        s = sum;
        z = ~(|sum);
        v = (a[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & xb[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & ~sum[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)]) | (~a[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & ~xb[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & sum[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)]);
        n = sum[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)];
    end
    
    
endmodule